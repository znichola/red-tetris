/** @typedef {import("./DTOs.js").Vector }	Vector */

/** @type Vector */
export const MinGameGridDimensions = Object.freeze({
  x: 5,
  y: 6,
});

/** @type Vector */
export const MaxGameGridDimensions = Object.freeze({
  x: 20,
  y: 30,
});

/** @type Vector */
export const DefaultGameGridDimensions = Object.freeze({
  x: 10,
  y: 20,
});
